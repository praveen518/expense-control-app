export type Theme = {
    /* Brand */
    primary: string;
    primarySoft: string;
  
    /* Surfaces */
    background: string;
    surface: string;
    surfaceSoft: string;
  
    /* Text */
    textPrimary: string;
    textMuted: string;
  
    /* Misc */
    divider: string;
    danger: string;
  };
  